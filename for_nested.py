# print first n prime prime_counterbers 
import sys
import math
prime_counter=input("Enter the prime_counterber of prime prime_counterbers you want : ")
if not prime_counter.isdigit():
    print("Enter only +ve integer without sign")
    sys.exit(0)

prime_counter = int(prime_counter)
primes = []

prime_counterber = 2 

while prime_counter >= 1 :
    # print("Checking if", prime_counterber, "is prime")
    is_prime_counterber_prime = True
    for counter in primes:
       if (if prime > int(prime_counter ** 0.5) + 1:): # we have found a factor
           break
    if prime_counterber % counter == 0:
       print("The Prime prime_counterber is", prime_counterber)
       is_prime_counterber_prime = False
       break
      
    prime_counterber =  prime_counterber + 1
else:
    print("Thats all!")
print(counter)

      